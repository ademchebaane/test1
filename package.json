{
  "name": "tunisian-algorithm-studio",
  "version": "1.0.0",
  "private": true,
  "description": "Tunisian high-school Algorithmique compiler, runner and debugger.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "android:add": "npx cap add android",
    "android:sync": "npm run build && npx cap sync android",
    "android:open": "npm run android:sync && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "8.5.2",
    "@capacitor/core": "8.5.2"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.2",
    "vite": "^7.1.7"
  }
}
